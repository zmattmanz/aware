#pragma once
// Plus Jakarta Sans Bold 30 px — VLW data placeholder (digits + . -)
// Generate: Processing VLW export (PJS Bold, 30pt, chars: 0123456789.-+) → rename array pjs_num[]
// Then: fontNum() -> cv.loadFont(pjs_num); cv.setTextSize( 1 );
static const uint8_t pjs_num[] __attribute__((unused)) = {0,0,0,0};  // stub
